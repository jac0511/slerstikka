<% @page_title = "Kirjautuminen" %>

<h2>Kirjautuminen</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>


<table>
	<tr>
		<th><%= f.label :email, "Sähköposti" %></th>
		<td><%= f.text_field :email, autofocus: true %></td>
	</tr>
	<tr>
		<th><%= f.label :password, "Salasana" %></th>
		<td><%= f.password_field :password, autocomplete: "off" %></td>
	</tr>
	<% if devise_mapping.rememberable? %>
	<tr>
		<th><%= f.label :remember_me, "Muista" %></th>
		<td><%= f.check_box :remember_me %></td>
	</tr>
	<% end %>
</table>

  <div class="actions">
    <%= submit_tag("Sisään!", :class => 'submit-tag') %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
